sudo apt-get install nodejs

sudo apt-get install npm

sudo npm install -g n

sudo n latest

hash -r

создание папки app в root и переброс docker-compose.yml

docker compose up

git clone https://github.com/Brondee/tg-order-web-app-back.git

npm i prisma

cd tg-order-web-app-back

docker start database-container

npx prisma migrate dev

sudo apt-get install snapd
sudo snap install core; sudo snap refresh core
sudo snap install --classic certbot
sudo ln -s /snap/bin/certbot /usr/bin/certbot

sudo apt autoremove

sudo apt install nginx

sudo certbot --nginx

загрузить в etc/nginx/sites-available default файл

sudo service nginx restart

docker compose up


